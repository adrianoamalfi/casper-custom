<!DOCTYPE html>
<html lang="{{@site.locale}}" data-default-color-scheme="{{#match @custom.color_scheme "Dark"}}dark{{else match @custom.color_scheme "Auto"}}auto{{else}}light{{/match}}"{{#match @custom.color_scheme "Dark"}} class="dark-mode"{{else match @custom.color_scheme "Auto"}} class="auto-color"{{/match}}>
<head>

    {{!-- Basic meta - advanced meta is output with {ghost_head} below --}}
    <title>{{meta_title}}</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    {{!-- Preload styles --}}
    <link rel="preload" as="style" href="{{asset "built/screen.css"}}" />

    {{!-- Theme assets - use the {asset} helper to reference styles & scripts,
    this will take care of caching and cache-busting automatically --}}
    <link rel="stylesheet" type="text/css" href="{{asset "built/screen.css"}}" />

    {{!-- LCP preload: cover image on homepage, feature image on posts --}}
    {{#is "home"}}
        {{#if @custom.show_publication_cover}}
            {{#if @site.cover_image}}
                <link rel="preload" as="image"
                      href="{{img_url @site.cover_image size="l" format="webp"}}"
                      imagesrcset="{{img_url @site.cover_image size="s" format="webp"}} 300w,
                                   {{img_url @site.cover_image size="m" format="webp"}} 600w,
                                   {{img_url @site.cover_image size="l" format="webp"}} 1000w,
                                   {{img_url @site.cover_image size="xl" format="webp"}} 2000w"
                      imagesizes="100vw"
                      fetchpriority="high" />
            {{/if}}
        {{/if}}
    {{/is}}
    {{#is "post, page"}}
        {{#post}}
            {{#if feature_image}}
                <link rel="preload" as="image"
                      href="{{img_url feature_image size="l" format="webp"}}"
                      imagesrcset="{{img_url feature_image size="s" format="webp"}} 300w,
                                   {{img_url feature_image size="m" format="webp"}} 600w,
                                   {{img_url feature_image size="l" format="webp"}} 1000w,
                                   {{img_url feature_image size="xl" format="webp"}} 2000w"
                      imagesizes="(max-width: 1200px) 100vw, 1200px"
                      fetchpriority="high" />
            {{/if}}
        {{/post}}
    {{/is}}

    <script>
        (function () {
            var root = document.documentElement;
            var stored = null;

            try {
                stored = window.localStorage.getItem('aa-theme-preference');
            } catch (e) { return; }

            if (!stored || ['light', 'dark', 'auto'].indexOf(stored) === -1) {
                return;
            }

            root.classList.remove('dark-mode', 'auto-color');

            if (stored === 'dark') {
                root.classList.add('dark-mode');
            } else if (stored === 'auto') {
                root.classList.add('auto-color');
            }
        })();
    </script>

    {{!-- Preconnect to third-party origins used by analytics/privacy scripts (~130ms saving each) --}}
    <link rel="preconnect" href="https://stats.adrianoamalfi.com">
    <link rel="dns-prefetch" href="https://stats.adrianoamalfi.com">
    <link rel="preconnect" href="https://nocookies.adrianoamalfi.com">
    <link rel="dns-prefetch" href="https://nocookies.adrianoamalfi.com">

    {{!-- Slot for per-template <meta> overrides (e.g. custom index pages) --}}
    {{#block "head_extra"}}{{/block}}

    {{!-- This tag outputs all your advanced SEO meta, structured data, and other important settings,
    it should always be the last tag before the closing head tag --}}
    {{ghost_head}}

</head>
<body class="{{body_class}} is-head-{{#match @custom.navigation_layout "Logo on cover"}}left-logo{{else match @custom.navigation_layout "Logo in the middle"}}middle-logo{{else}}stacked{{/match}}{{#match @custom.title_font "=" "Elegant serif"}} has-serif-title{{/match}}{{#match @custom.body_font "=" "Modern sans-serif"}} has-sans-body{{/match}}{{#if @custom.show_publication_cover}} has-cover{{/if}}"
      data-anchor-label="{{t "Link diretto a"}}"
      data-copy-label="{{t "Copia"}}"
      data-copy-done-label="{{t "Copiato"}}">
<div class="viewport">

    <header id="gh-head" class="gh-head outer{{#match @custom.header_style "Hidden"}} is-header-hidden{{/match}}">
        <div class="gh-head-inner inner">
            <div class="gh-head-brand">
                <a class="gh-head-logo{{#unless @site.logo}} no-image{{/unless}}" href="{{@site.url}}">
                    {{#if @site.logo}}
                        {{> "media/picture-ui"
                            image=@site.logo
                            picture_class="aa-site-logo-picture aa-responsive-picture"
                            sizes="(max-width: 767px) 132px, 160px"
                            loading="eager"
                            decoding="async"
                            alt=@site.title
                        }}
                    {{else}}
                        {{@site.title}}
                    {{/if}}
                </a>
                <button class="gh-search gh-icon-btn" aria-label="{{t "Search this site"}}" data-ghost-search>{{> "icons/search"}}</button>
                <button class="gh-burger" aria-label="{{t "Toggle menu"}}"></button>
            </div>

            <nav class="gh-head-menu">
                {{navigation}}
                {{#unless @site.members_enabled}}
                    {{#match @custom.navigation_layout "Stacked"}}
                        <button class="gh-search gh-icon-btn" aria-label="{{t "Search this site"}}" data-ghost-search>{{> "icons/search"}}</button>
                    {{/match}}
                {{/unless}}
            </nav>

            <div class="gh-head-actions">
                <div class="aa-theme-switcher" data-theme-switcher>
                    <div class="aa-theme-switcher-controls" role="group" aria-label="{{t "Selettore tema"}}">
                        <button class="aa-theme-option" type="button" data-theme-option="light" aria-pressed="false" aria-label="Tema chiaro">
                            {{> "icons/theme-light"}}
                        </button>
                        <button class="aa-theme-option" type="button" data-theme-option="dark" aria-pressed="false" aria-label="Tema scuro">
                            {{> "icons/theme-dark"}}
                        </button>
                        <button class="aa-theme-option" type="button" data-theme-option="auto" aria-pressed="false" aria-label="Tema automatico">
                            {{> "icons/theme-auto"}}
                        </button>
                    </div>
                </div>
                {{#if @custom.brand_footer_linkedin_url}}
                    <a class="gh-icon-btn aa-header-social" href="{{@custom.brand_footer_linkedin_url}}" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn" title="LinkedIn">
                        {{> "icons/linkedin"}}
                    </a>
                {{/if}}
                {{#if @custom.brand_footer_github_url}}
                    <a class="gh-icon-btn aa-header-social" href="{{@custom.brand_footer_github_url}}" target="_blank" rel="noopener noreferrer" aria-label="GitHub" title="GitHub">
                        {{> "icons/github"}}
                    </a>
                {{/if}}
                {{#unless @site.members_enabled}}
                    {{^match @custom.navigation_layout "Stacked"}}
                        <button class="gh-search gh-icon-btn" aria-label="{{t "Search this site"}}" data-ghost-search>{{> "icons/search"}}</button>
                    {{/match}}
                {{else}}
                    <button class="gh-search gh-icon-btn" aria-label="{{t "Search this site"}}" data-ghost-search>{{> "icons/search"}}</button>
                    <div class="gh-head-members">
                        {{#unless @member}}
                            {{#unless @site.members_invite_only}}
                                <a class="gh-head-link" href="#/portal/signin" data-portal="signin">{{t "Sign in"}}</a>
                                <a class="gh-head-button" href="#/portal/signup" data-portal="signup">{{t "Subscribe"}}</a>
                            {{else}}
                                <a class="gh-head-button" href="#/portal/signin" data-portal="signin">{{t "Sign in"}}</a>
                            {{/unless}}
                        {{else}}
                            <a class="gh-head-button" href="#/portal/account" data-portal="account">{{t "Account"}}</a>
                        {{/unless}}
                    </div>
                {{/unless}}
            </div>
        </div>
    </header>

    <div class="site-content">
        {{!-- All other templates get inserted here, index.hbs, post.hbs, etc --}}
        {{{body}}}
    </div>

    {{!-- Footer --}}
    <footer class="site-footer outer">
        <div class="inner aa-footer-grid">
            <section class="aa-footer-brand">
                {{> "brand/footer-signature"}}
            </section>
            <div class="aa-footer-meta">
                <nav class="site-footer-nav">
                    {{navigation type="secondary"}}
                </nav>
                <section class="copyright"><a href="{{@site.url}}">{{@site.title}}</a> &copy; {{date format="YYYY"}}</section>
            </div>
        </div>
    </footer>

    {{!-- Back to top button --}}
    <button id="aa-back-top" class="aa-back-top" aria-label="{{t "Torna in cima"}}">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="18 15 12 9 6 15"></polyline>
        </svg>
    </button>

</div>
{{!-- /.viewport --}}

{{#is "post, page"}}
    {{> "lightbox"}}
{{/is}}

{{!-- Scripts - handle responsive media, menus, infinite scroll, floating headers, and galleries --}}
<script defer src="{{asset "built/core.js"}}"></script>
{{#is "post, page"}}
<script defer src="{{asset "built/post.js"}}"></script>
<script>
    (function () {
        function loadScript(src) {
            var script = document.createElement('script');
            script.src = src;
            script.defer = true;
            document.body.appendChild(script);
        }

        if (document.querySelector('.gh-content pre code')) {
            loadScript('{{asset "built/code.js"}}');
        }

        if (document.querySelector('.kg-image-card > .kg-image[width][height], .kg-gallery-image > img, .aa-lightbox-trigger')) {
            loadScript('{{asset "built/lightbox.js"}}');
        }
    })();
</script>
{{/is}}

{{!-- Ghost outputs required functional scripts with this tag - it should always be the last thing before the closing body tag --}}
{{ghost_foot}}

</body>
</html>
